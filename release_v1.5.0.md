CorrectNote は、誤字を気にせず思考の速度でメモが取れることを目指した Windows 用のメモ帳です。完全にオフラインで動き、辞書とローカルの統計だけで補正します。

v1.4.0 からの変更点です。**補正が届く範囲を広げた版**であり、あわせて **F2 の範囲の切り方**と**品詞判定の言い方**を全面的に見直しました。

---

## 直せる誤りが増えました

手元の未解決リスト（33行・**初期状態**＝語彙を育てていない、入れたばかりの状態）で数えた結果です。

    かな入力     完全に直る **15 → 19 行**（＋読みまで届く 1 行）
    ローマ字入力 完全に直る **14 → 17 行**（＋読みまで届く 1 行）

新しく直るようになったもの:

    解す咳が終わった      → 解析が終わった
    田部井号して          → タブ移動して
    スクロール語の見えた  → スクロール後の見えた
    貸す九人              → 確認
    札ん港になる          → 参考になる
    右田でブルクリック    → 右ダブルクリック
    乳リュク見ています    → 入力見ています
    文字乳リュク          → 文字入力
    も水戸に戻ります      → もとに戻ります（読みまで）

### どこを変えたのか

- **助詞のところで区切って考えます** — `かいすせきがおわった` のように、かなが長く続いて全体では読めない並びを、`が`・`に` のような助詞で区切ってから直します（区切ってよい形かどうかは5つの条件で確かめます）
- **「品詞の並びが変」を、直す手がかりに使います** — `解す咳`（動詞の終止形に名詞が直付き）のように**日本語として成り立たない並び**のときだけ、余分に打ったキーを落とす手まで開きます。正しい並び（`糸を察して`）には走りません
- **漢字の読みの表を厚くしました** — 音訓の表を 1,433 字／1,564 読みぶん埋め、2字の漢語の「よく見る度合い」の表を新しく同梱しました。読みの組み立てと、候補の並べ方に効きます
- **カタカナの断片を、塊に含めて考えます** — `文字乳リュク` の `リュク` のように**読みの立たないカタカナ**は、一つの語を漢字とカタカナで切ってしまった跡なので、まとめて開き直します。読める語（`ソフトウエア`・`プレイ動画`）は今までどおり触りません
- **カタカナのサ変名詞に「語」は付きません** — `スクロール語` は `スクロール後` に。`ドイツ語`・`検索語`・`カタカナ語` のような正しい語は守ります
- **口語の縮約や「ら抜き」を、正しい日本語として扱います**（`してる`・`食べれる`）——異様と見なして壊さないようにしました

## F2 の範囲（クリックで選ばれる単位）を直しました

- **小書きの文字で範囲が始まらない**ようにしました（`に｜ゅうりょく` のような切れ方をしません）
- **`を`・`に` などの助詞で区切ります**（`つつぎをはなす` が `ぎをはなす` で選ばれていました）
- **長すぎる範囲を、知っている語で切り直します**（`きょじえかくらん` 全体ではなく `きょじえ｜かくらん`）
- **語幹と語尾をまとめた範囲は、読みもまとめます** — `たぶい` をクリックしたときに、候補が**まるごと `たぶ` の話**（同音の語＝タブ、かな表記＝たぶ）になっていました
- **候補一覧の高さ**が足りず、下の数行が画面の外に出ていたのを直しました

## 品詞判定の言い方を、全面的に見直しました

（表示メニューの「補正候補に品詞の判定を表示する」をオンにしたときに出るものです。**補正の答えは1文字も変わりません。**）

手元のメモに出てくる単位を全部（**38,028 か所・5,471 種**）書き出して1つずつ読み、**1,156 種・4,640 か所**の言い方を直しました。

- **文の中での判定を使います** — これまでは選んだ範囲**だけ**を切り離して解析し直していたので、文の中では起きなかった分かれ方が出ていました

      日間  →「日 ＝ 固有名詞（地名・国名） ／ 間 ＝ 接尾辞」だった
      かな  →「か ＝ 助詞（副助詞） ／ な ＝ 助詞（終助詞）」だった
      ない  →「イ形容詞」だった（`できない` の `ない` は助動詞です）
      よう  →「感動詞」だった

- **推測を、判定のように書きません**

      問題      →「イ形容詞の語幹」ではなく **名詞（「ない」に続く）**
      the・Ctrl →「固有名詞（組織名）」ではなく **英字**
                  （解析は知らない英字を片端から組織名と推測します）
      `://`     →「名詞（サ変）」ではなく **記号**

- **1拍の内側で切れた範囲には、品詞を書きません** — `しゅ` は1拍で、`し` はその半分です。半分の音に品詞はありません

      しゅるい → 「し ＝ 動詞・連用形／原形 する」と書いていた
               → **判定できません（`しゅ` は1拍——語の途中で切れています）**

- **接尾辞をどんな接尾辞か書き分けます**（助数詞・ナ形容詞を作る・サ変…）
- 自動補正が入った行では、そもそも品詞を運んでいなかったのも直しました

## 直した不具合

- **簡易入力の窓が、改行しても縦に伸びないことがありました**（折り返した行を数えていませんでした）
- **正しく書いた文が、同じ行に書いた「間違いの例」に引きずられて壊れる**ことがありました。不具合を `A → B` の形でメモに書き残すと、壊れた形 B のほうが証拠になっていました

      意図を察して → 意図をさして  に壊れていた
      ひとつ前より前も… → ひとつ前よ前も…  に壊れていた

  文法として完成した形（連用形＋`て`）と、正しく働いている助詞は、書き換えないようにしました
- 正しい語に補正が走っていたものを、ほかにもいくつか塞ぎました（`一番下に` → `一番化に`、`変換中` → `変換かな` など）

## 説明書について

ファイル名から版番号を外しました（`CorrectNote_説明書.html`）。**アプリの版が上がったことを見て書き出し直す**ようにしたので、更新のたびに説明書が増えることはありません。v1.4.0 までにできた `CorrectNote_説明書v5.html`・`v6.html` は、次の起動のときに片付きます。

CorrectNote は、誤字を気にせず思考の速度でメモが取れることを目指した Windows 用のメモ帳です。完全にオフラインで動き、辞書とローカルの統計だけで補正します。

v1.3.0 からの変更点です。**「どうしてそう直したのか」が画面から見えるようになった版**であり、あわせて**起動するたびに答えが変わっていた不具合**を直しました。

---

## 新しくできること

### 補正候補に「品詞の判定」と「補正の根拠」を出せます（どちらも既定オフ）

表示メニューに2つ増えました。

- **補正候補に品詞の判定を表示する** — 候補一覧のいちばん下に「－ 品詞判定 －」が出て、選んだ範囲がどう解析されたかを書きます。「イ形容詞」「ナ形容詞の語幹」のように、日本語の呼び方で出します。活用している語には**原形**を添えます（`走っ ＝ 動詞・連用形（た接続）／原形 走る`）。辞書に無い語には**（辞書に無い語）**と必ず書くので、アプリが推測しただけの場所が分かります。
- **補正候補に根拠を表示する** — その下に「－ 補正根拠 －」が出て、**なぜ不自然だと見たのか／何をして直したのか／学習履歴が効いたか**を書きます。

補正の答えそのものは1文字も変わりません。**うまく直らないときに、どこで止まっているかを見るためのもの**です。

### 説明書を v6 に更新しました

上の2つのメニューの説明を書き足しています。**ファイル名が変わったので、初回起動時に新しい説明書が書き出されます**（古い `CorrectNote_説明書v5.html` は消していただいて構いません）。

---

## 直した不具合

### ★ 起動するたびに、同じ文の直し方が変わることがありました

読みを語の列に組み直す処理が、**候補を数え上げる途中で打ち切っていて、どれが残るかが起動ごとに変わって**いました。順番を決めてから打ち切るように直したので、**同じ入力には毎回同じ答え**を返します。

    きんて → きて  になったり  消えて  になったり
    さんとう → そのまま  になったり  山道  になったり

### 赤い補正範囲が、タブの空白まで塗られていました

行を直したあとに範囲を割り出す処理が**タブを跨いで**いました。10か所あった同じ処理を1つにまとめ、**空白を跨がない**ようにしました。

### タブの幅が、直したあと短いままになることがありました

結果の欄を書き直すと、その行の印がまれに落ちることがあり、塗り直す側が「中身が同じなら何もしない」で早く帰っていました。

### 候補一覧の説明が、途中で切れて見えませんでした

候補の多い語では、いちばん下に足した説明が一覧の外へ出てしまい、キーボードでも届きませんでした。説明の行数ぶん高さを伸ばしました。

### 正しく書いた文を壊していたもの

- `以下の2つがある` → `以下の2つかえる`（数字の直後の並びに手を入れていた）
- `一番下に` → `一番化に`
- `おせわになりました` の「お」を別の語と読んでいた
- `切り替え時` → `切り返し` ／ `鍵括弧` → `過括弧` ／ `語彙読込` → `語彙詠込め`

---

## 補正が当たるようになりました

### 打ったつもりの文字に、より届くようになりました

    がいしょつする → 外出する      （小さい「ゅ」の押し間違い）
    すきにん      → 確認
    ひらんがな    → 平仮名
    しゅうりょじ  → 終了時
    かくにん      → 確認
    さいだいか    → 最大化
    引き月資料    → 引き継ぎ資料
    乳リュク見ています → 入力見ています
    やん後の繋がり → 単語の繋がり
    おくゆくこてい → おくゆきこてい

### ローマ字入力のときは、ローマ字のキーの近さで見ます

これまで、ローマ字で打っていても**かなキーボードの並び**で「隣のキー」を測っていました。打った人が触っていない配列で近いことは根拠にならないので、**入力方式に合わせて見る**ようにしました。誤変換が減っています。

### 助詞の抜けを補えるようになりました

    静か歩く            → 静かに歩く
    アクティブなっていない → アクティブになっていない
    そのままに市内       → そのままにしない

### 漢字を「ひらがなに開き戻す」道ができました

これまでの補正はすべて「かなを漢字に」の向きだけで、**漢字をかなに戻す道がありませんでした**。

    設計の見して、 → 設計のみして

---

## 「正しく書けているのに変えてしまう」を、さらに減らしました

|  | v1.3.0 | v1.4.0 |
|---|---|---|
| 直った（ローマ字） | 1887 | **1949** |
| 化けた（ローマ字） | 103 | **99** |
| 直った（かな） | 1885 | **1948** |
| 化けた（かな） | 106 | **105** |
| 正しい文を壊す | 0 | **0** |
| 覚えた語を壊す | 1 | **0** |

（3,481語をわざと壊して測った数です。「化けた」のうち2件は、`けんざく → 検索`・`かいびょう → 開票` のように**正しい漢字なのに数え方の都合で化け扱い**になっているものです）

---

## 「不自然な文字列」の印（紫）を、文法で見るようにしました

紫の印は「ここは打ち間違いかもしれない」という**気づきの印**です。今回、**日本語の文法そのもの**で判定するようにしました。

- **活用形の続き方** — `書け`（仮定形）と `食べ`（連用形）はどちらも動詞ですが、後ろに来られるものが違います
- **接尾辞の続き方** — `田中さん行く` は `田中さん**が**行く` の助詞落ち
- **ナ形容詞の語幹の続き方** — `静か歩く` は `静か**に**歩く`
- **副詞＋に の続き方** — `そのままに` の後ろには用言が要ります

同時に、**正しい文に付いていた印を減らしました**（誤って付く印が 179 → 107 まで減りました）。

    元気出して！   ← 「元気**を**出して」の助詞落ち。正しい
    大変助かる     ← 「大変」は副詞としても使う。正しい
    1行分開いていて ← 数量の言い方。正しい
    急がば回れ     ← 文語。正しい
    とはいえ       ← 4文字で1つの接続詞

---

## ダウンロードと使い方

下の **Assets** から `CorrectNote.exe` をダウンロードし、好きなフォルダに置いて実行するだけです。Python のインストールやセットアップは不要です。語彙などのデータファイルは exe と同じフォルダに自動で作られるので、フォルダごと移動すればそのまま持ち運べます。

**v1.3.0 から入れ替える場合**は、exe を差し替えるだけで、それまでのメモと覚えた語彙はそのまま引き継がれます。**入れ替えたあとは一度アプリを閉じて起動し直してください**（補正エンジンが変わっているので、前回の解析結果の控えを作り直します）。

説明書は初回起動時に exe と同じフォルダへ書き出されます（`CorrectNote_説明書v6.html`）。メニューの「このアプリについて → 説明書をHTMLで展開」からいつでも開けます。

## 安全性について

このアプリは**完全にオフラインで動作**します。ネットワーク通信を行うコードが存在せず、AI やクラウドへの送信は一切ありません。データはすべてローカルの JSON ファイルに保存され、グローバルキーボードフックも使いません。補正は辞書（janome の形態素解析）とローカルの統計だけで行います。

## ライセンス

GNU General Public License v3.0 で公開しています。

Copyright (C) 2026 Takahashi Yuu

CorrectNote は、誤字を気にせず思考の速度でメモが取れることを目指した Windows 用のメモ帳です。再変換・簡易入力・自動補正などで、効率よく文字を打てるようにします。かな入力(JIS 配列)とローマ字入力のどちらにも対応しており、半角アルファベットの入力への対応も進めています。

## ダウンロードと使い方

下の **Assets** から `CorrectNote.exe` をダウンロードし、好きなフォルダに置いて実行するだけです。Python のインストールやセットアップは不要です。語彙などのデータファイルは exe と同じフォルダに自動で作られるので、フォルダごと移動すればそのまま持ち運べます。

## 安全性について

このアプリは**完全にオフラインで動作**します。

- **ネットワーク通信を行うコードが存在しません**。AI やクラウドへの送信は一切なく、入力した内容がパソコンの外に出ることはありません。ソースコードを検索して `socket` / `urllib` / `requests` などの通信モジュールが使われていないことを、誰でも確認できます。
- **データはすべてローカルの JSON ファイル**に保存されます。メモの内容も語彙も、あなたのパソコンの中にだけあります。
- **グローバルキーボードフックを使いません**。ホットキーはアプリ自身のウィンドウ内でのみ動作する実装で、他のアプリの入力を監視することはありません。この設計はウイルス対策ソフトの誤検知も起こしにくくしています(同じ理由で、exe の圧縮に誤検知の原因となる UPX も使っていません)。
- 補正は**辞書(janome の形態素解析)とローカルの統計**だけで行います。

## 主な機能

- **再変換**: 確定済みの単語を、いつでも候補一覧から選び直せます。クリックのほか **F2** キーでも開けます。一覧が開いているあいだは **←** **→** で対象の語そのものを前後に移せます。
- **簡易入力ウィンドウ**: **Ctrl+Insert** または **Ctrl+Shift+-** で、他のアプリを使っている最中でも小さな下書き窓を呼び出せます。閉じると内容が自動でクリップボードに入り、本体のメモ帳にも送られます。
- **自動補正**: 隣のキーの押し間違い、濁点・半濁点の付け忘れ、小書きの忘れ、打ち忘れや押しすぎ、打つ順番の入れ替わり、日本語入力をオフにしたまま打った行などを直します。
- **ブックマーク / 引用モード / 括弧で括る / タブ管理と自動保存**、検索・置換、ダークモードなど。

直すのは「明確に誤りだと言えるもの」だけです。判断がつかない場合はあえて直さず、色を付けて知らせるだけにとどめます。

## ライセンス

GNU General Public License v3.0 で公開しています。改変・組み込みして配布する場合は、その配布物のソースコードも同じライセンスで公開する必要があります。

Copyright (C) 2026 Takahashi Yuu

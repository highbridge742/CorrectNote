CorrectNote は、誤字を気にせず思考の速度でメモが取れることを目指した Windows 用のメモ帳です。完全にオフラインで動き、辞書とローカルの統計だけで補正します。

v1.2.1 からの変更点です。**補正の中身がいちばん大きく変わった版です。**

---

## 新しくできること

### 俯瞰 ― 右ダブルクリックで全体を見渡す

メモ欄（または補正結果欄）で**右ボタンをダブルクリックし、2回目を押したまま保つ**と、文字が一時的にとても小さくなり、行間も詰まって、広い範囲が一度に見えます。

- そのまま**マウスを上下に動かすとスクロール**します
- **四角い枠**が、いまボタンを離したときに画面へ映る範囲を示します
- ボタンを離すと文字の大きさが元に戻り、**俯瞰で見ていた行がそのまま画面に残ります**

長いメモの中で目当ての場所へ一気に飛ぶための機能です。

### 簡易入力ウィンドウでも、補正が自動で反映されます（既定オン）

これまで簡易入力ウィンドウは補正の色を出すだけでしたが、**欄の中の文字そのものを直す**ようになりました。本体の「ひとつにまとめた表示」と同じ仕組みで、F2 の選び直しも効きます。

- 日本語入力の**変換中は見送ります**（未確定の文字を壊しません）
- カーソルは書き換えたあとも同じ場所に残ります
- **Ctrl+Z で元に戻せます**

表示メニューの「簡易入力で、補正を自動で反映する」で切り替えられます。

### アプリのアイコンが付きました

ノートに補正が入っていく形の絵です。exe そのもの・ウィンドウ・タスクバー・**Alt+Tab の切り替え画面**に出ます。

### タブキーの幅

タブ1回で**全角1文字ぶん以上**必ず進むようにしました。これまでは打った位置しだいで半角1文字ぶんしか開かず、見えないことがありました。行どうしの列もゆるく揃います。

### 説明書を v5 に更新しました

俯瞰・タブ移動のショートカット（Ctrl+PageUp / Ctrl+PageDown ほか）・紫の意味を書き足しています。

---

## 直した不具合（画面まわり）

- **最大化したときにタスクバーを隠していた**のを直しました。上の端へのドラッグで最大化したあと、タスクバーから最小化して戻したときも隠しません
- **簡易入力で打つと、未変換の文字が画面の左上にも出て交互に点滅する**のを直しました（本体と簡易入力が日本語入力の文脈を共有していたため）
- **解析中に、空白やタブの印がちらついていた**のを直しました。あわせて、画面に見えている範囲の解析が済むまでの待ち時間が**大きく短くなりました**
- タブを切り替えて戻ると解析をやり直していたのを直しました（語彙を覚えても、他のタブの控えを捨てません）
- **タブ文字のある文書で動作が重かった**のを直しました
- 余白の右ドラッグによるスクロールを作り直しました。掴んでいる間はカーソルを隠し、画面の端に着く前に戻すので、長い距離を続けて動かせます
- 解析中にスクロールした先へ、補正結果がすぐ出るようにしました
- F2 の再変換まわりの取りこぼしを直しました（候補の出ない語を飛ばして遡る、一覧が閉じていても左右キーで渡り歩ける、など）

---

## 補正が当たるようになりました

### 打ち間違いのパターンを増やしました

- **場違いな小書き**を隣のキーの文字に戻します（`上に爪、` → `上に詰め、`）
- **場違いな濁点**を隣のキーの文字に戻します（`もじ゛つ` → `もじれつ`）
- **伸ばし棒（ー）の打ち間違い**を隣のキーで戻します
- **濁点が1つ隣にずれた**形を直します（`つつぎをはなす` → `続きを話す`）
- **同じ2文字を2回くり返す形**（そもそも・わくわく）は、型そのものを手がかりに直します（`そももそ` → `そもそも`）
- **音便のあとの た／だ** を直します（`読んて` → `読んで`、`泳いた` → `泳いだ`）
- **Shift の押し忘れ**で大書きになった拗音を直します（`にゆうりよくみす` → `にゅうりょくみす`）
- **`\` は「ろ」と「ー」の2つのキー**であることを踏まえて直します（`rh¥¥.` → `スクロール`）
- **大文字の略語の隣のキー**を直します（`YRLは` → `URLは`）

### 漢字の並びから、本来の入力を探れるようになりました

- **読みを繋ぐと1語になる**形を直します（`待ち外の補正` → `間違いの補正`）
- **読みを語の境目で切り直します**（`切り替え時二階席が走っていて` → `切り替え時に解析が走っていて`）
- **助詞をはさんだ読みを1語へ寄せます**（`全体と押して` → `全体通して`、`長いことで来ません` → `長いことできません`）
- **接尾の枠を保ったまま**足りない字を補います（`囚虜時` → `終了時`）
- **見本が無くても、かなの連なりを組み直します**（`かなうちでのほせい` → `かな打ちでの補正`、`かんいにゆうりゆく` → `簡易入力`）
- **同じ行に `誤 ⇒ 正` の形で並記があれば、そこから学びます**（逆向きにも対応）
- **「〜中か」に続く同音の対**を2か所まとめて直します（`上昇中か加工中華` → `上昇中か下降中か`）

### 日本語の知識を厚くしました

- **漢字の音訓表**を 1,052字/1,274読み → **2,757字/4,423読み** に増やしました
- **同音異義語の対の表**を新しく持ちました
- **品詞と活用の文法**を新設し、「い段＝連用形」「て＋補助動詞」などの活用を辿れるようにしました。これまでは活用を知らないまま「説明が付かない」と判定していました
- **読みの文字3連の表**（113,203種）を持ちました

---

## 「正しく書けているのに変えてしまう」を、さらに減らしました

補正が増えたぶん、行き過ぎを塞ぐ作業も同じだけ行いました。

- **できあがっている自然な文には、どの補正も走らせません**。判定の入口を1か所にまとめ、道ごとに別々の断り方をしていたのをやめました
- 副詞・接尾辞・文節の末尾・中黒・「させていく」のような形を守ります
- **本人が選んだ表記**は、その語を実際に使った実績があるときだけ守るように改めました
- 正しい日本語 1,500 文での誤検知 0 件・実機のメモ全行での取りこぼし 0 行を保っています

## 紫（不自然な文字列）の判定を広げました

紫は「**CorrectNote が補正しようとしたが、補正する文字列を見つけられなかった**」という印です。

- **ひらがなが続く箇所**の異様さを、品詞と活用の文法で判定できるようになりました
- **実績のない姓＋姓**、**実績のない姓＋用言**にも印を立てます
- 印を出すだけで、補正の答えは1文字も変えていません

---

## ダウンロードと使い方

下の **Assets** から `CorrectNote.exe` をダウンロードし、好きなフォルダに置いて実行するだけです。Python のインストールやセットアップは不要です。語彙などのデータファイルは exe と同じフォルダに自動で作られるので、フォルダごと移動すればそのまま持ち運べます。

**v1.2.1 から入れ替える場合**は、exe を差し替えるだけで、それまでのメモと覚えた語彙はそのまま引き継がれます。**入れ替えたあとは一度アプリを閉じて起動し直してください**（補正エンジンが変わっているので、前回の解析結果の控えを作り直します）。

説明書は初回起動時に exe と同じフォルダへ書き出されます（`CorrectNote_説明書v5.html`）。メニューの「このアプリについて → 説明書をHTMLで展開」からいつでも開けます。

## 安全性について

このアプリは**完全にオフラインで動作**します。ネットワーク通信を行うコードが存在せず、AI やクラウドへの送信は一切ありません。データはすべてローカルの JSON ファイルに保存され、グローバルキーボードフックも使いません。補正は辞書（janome の形態素解析）とローカルの統計だけで行います。

## ライセンス

GNU General Public License v3.0 で公開しています。

Copyright (C) 2026 Takahashi Yuu

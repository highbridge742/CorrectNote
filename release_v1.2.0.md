CorrectNote は、誤字を気にせず思考の速度でメモが取れることを目指した Windows 用のメモ帳です。完全にオフラインで動き、辞書とローカルの統計だけで補正します。

v1.1.0 からの変更点です。

---

## 見えない空白を表示できるようになりました（新機能・既定はオン）

半角スペース・全角スペース・タブに、**行の下端の細い灰色の線**で印を付けます。

- **連続して入っていても、何個あるか数えられます**（濃淡を交互に付けています）
- **行の折り返しの直前でも、半角か全角かが幅で分かります**
- 印は背景に近い灰色なので、読む邪魔になりません
- **簡易入力ウィンドウでも同じように出ます**（切り替えは本体と共通）

表示メニューの「見えない空白（半角・全角・タブ）を表示」で、いつでも切り替えられます。

## スクロールした先の補正結果が、すぐ出るようになりました

下へスクロールして新しく見えた範囲を先に解析するようにしました。
手を止めてから補正欄に結果が出るまで、**1.10秒 → 0.35秒**（974行のメモで計測）。

## 補正が当たるようになりました

- **押しすぎ（同じキーの連打）** を畳んで直します
- **打つ順番の入れ替わり** を戻して直します
- ひらがなが続く箇所の直しが広がりました
- **IME で確定したときの「表記と、そのとき打った読み」を覚えて**、読みの逆算より先に使うようになりました
- **漢字の並びが日本語として異様なとき**、いったんひらがなに開いてから直し先を探す道を新しく入れました（例: `野外文章 → 長い文章`）
- **接尾の意味に合わない複合語**を直せるようになりました（例: `背景食 → 背景色`）
- シフトを押し損ねた拗音、カタカナが混じる語なども扱えるようになりました

## 「正しく書けているのに変えてしまう」を減らしました

行き過ぎた直しの経路を洗い出して、ひとつずつ塞ぎました。
末尾の助詞を食う、拗音を活用の断片と取り違える、といった形が無くなっています。
**判断がつかないものは触らない**という方針は変えていません。

## 補正が速くなりました

候補を探す仕組みを作り直し、無駄な総当たりを減らしました。

## 画面まわりの直し

- **単語をクリックする単位**が細かすぎたのを直しました（ひらがなの続きが1語で選べます）
- **F2 の遡り**が、候補の無い語で途切れなくなりました
- **行末より右の余白**をクリックすると、その行の末尾にカーソルが移ります
- **簡易入力から送った文字の色**が、打鍵で消えるようになりました（NumLock が入っていても戻ります）。2色の見分けも付けやすくしました
- **Ctrl+PageUp / Ctrl+PageDown** でタブを移れます
- **F1 の引用モード**が、左右に並べた表示でもメモ欄に印を出します
- テンキーの小数点で文字が消える不具合を直しました
- ダークモードで見分けの付きにくかった色を調整しました

## 配布物の直し

- **説明書（HTML）が exe に確実に入る**ようにしました
- exe と一緒に配るはずのものが抜けていないか、ビルドのたびに点検するようにしました

---

## ダウンロードと使い方

下の **Assets** から `CorrectNote.exe` をダウンロードし、好きなフォルダに置いて実行するだけです。Python のインストールやセットアップは不要です。語彙などのデータファイルは exe と同じフォルダに自動で作られるので、フォルダごと移動すればそのまま持ち運べます。

**v1.1.0 から入れ替える場合**は、exe を差し替えるだけで、それまでのメモと覚えた語彙はそのまま引き継がれます。

## 安全性について

このアプリは**完全にオフラインで動作**します。ネットワーク通信を行うコードが存在せず、AI やクラウドへの送信は一切ありません。データはすべてローカルの JSON ファイルに保存され、グローバルキーボードフックも使いません。補正は辞書（janome の形態素解析）とローカルの統計だけで行います。

## ライセンス

GNU General Public License v3.0 で公開しています。

Copyright (C) 2026 Takahashi Yuu

CorrectNote は、誤字を気にせず思考の速度でメモが取れることを目指した Windows 用のメモ帳です。完全にオフラインで動き、辞書とローカルの統計だけで補正します。

v1.2.0 からの変更点です。**機能の追加はありません。直しが目的の版です。**

---

## テンキーの小数点で文字が消える不具合を、今度こそ直しました

v1.2.0 の説明に「直しました」と書いていましたが、**直っていませんでした**。
日本語入力（IME）がオンのままテンキーの `.` を打つと、`.` が入らずにカーソル位置の1文字が消えていた件です。
原因は画面の部品の**キー束縛の優先順位**で、IME が確定した `.` を受け止める処理が一度も呼ばれなくなっていました。
本物の打鍵を送って確かめています（IME オン／オフ、本物の Delete）。

## 画面まわりの直し

- **本文より下の余白から左ドラッグ**すると、スクロールではなく**範囲選択**になります（マウスの場合。タッチの1本指スクロールはそのまま）
- **文章の終端に細い罫線**を引きます——最後の文字の行のひとつ下に出ます。どこまで書いたかがひと目で分かります
- 候補一覧が開いているときに IME で確定した記号が、一覧の操作に化けないようにしました

## 補正の進み（異様な文字列をそのままにしない）

- **判断に迷った箇所と同じ紫で、異様と見た範囲に色を付けます**（どこまで判定できているかが見えます）
- 拮抗した候補で手を引くのをやめ、**使用実績 → 同梱の表の一般的さ** の順で決めます（例: `キーード` → キーボード。本人がよく使う語があればそちら）
- 平仮名を平仮名に直します——同じ助詞の押しすぎ（`ににして` → `にして`）、隣のキーの巻き込み（`すねると` → `すると`）、かなで打った外来語（`かーそね` → カーソル）
- 漢字の並びが変でも、直前のかなと漢字の頭が繋がって語になり、残りが隣のキー1つで語に届くなら割り直します（`これからな学外しょつする` → `これから長く外出する`）
- **打った読みが語としてそのまま在り、いまの表記のほうが語として無いなら、表記を組み直します**（`待ち外の補正` → `間違いの補正`）。いまの表記が語として在るもの（例: `官僚`）は、本人が選んだものとして触りません
- **漢字の名詞に「し／する」が直に付いた形**（`田部井号して`・`誤字して`）も異様として色を付けます。`確認して` `安定して` `無効化して` `お渡しする` `ドラッグする` のような正しい形は除いてあります
- いずれも**正しく書いた語は動かしません**（正しい日本語 1,500 文での誤検知 0 件を保ったまま）

## 補正の一貫性

- 初期語彙で**同じ読みに表記が2つある語**（例: `つながり`／`繋がり`）の選ばれ方が、環境の時計の刻みで変わることがありました。**表の順で決まる**ようにし、どの環境でも同じ答えになります（Windows で出ていた答えは変わりません）
- 補正エンジンを入れ替えたあと、**前回の解析結果の控えに古い答えが残る**ことがあったのを直しました

## ビルドの直し

- GitHub Actions の Windows で、日本語の報告がビルドを止めていた件を直しました

---

## ダウンロードと使い方

下の **Assets** から `CorrectNote.exe` をダウンロードし、好きなフォルダに置いて実行するだけです。Python のインストールやセットアップは不要です。語彙などのデータファイルは exe と同じフォルダに自動で作られるので、フォルダごと移動すればそのまま持ち運べます。

**v1.2.0 から入れ替える場合**は、exe を差し替えるだけで、それまでのメモと覚えた語彙はそのまま引き継がれます。**入れ替えたあとは一度アプリを閉じて起動し直してください**（補正エンジンが変わっているので、前回の解析結果の控えを作り直します）。

## 安全性について

このアプリは**完全にオフラインで動作**します。ネットワーク通信を行うコードが存在せず、AI やクラウドへの送信は一切ありません。データはすべてローカルの JSON ファイルに保存され、グローバルキーボードフックも使いません。補正は辞書（janome の形態素解析）とローカルの統計だけで行います。

## ライセンス

GNU General Public License v3.0 で公開しています。

Copyright (C) 2026 Takahashi Yuu

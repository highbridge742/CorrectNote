# CorrectNote v1.8.0

v1.7.0からの変更点です。補正の判定から読みの復元、候補選択、補正後の確認まで、文脈を引き継ぐ処理を改善しました。

- 動詞・形容詞・助動詞・接尾辞の判定を改善しました。丁寧形、使役、受身、継続、複合動詞、口語の省略や縮約を前後の接続から確認します。
- 品詞を判定できない長いひらがなの文を、辞書で判定できる名詞句や述語の範囲へ区切って扱うようにしました。
- IME変換後の文字列を読みへ戻す際、保存済みの表記と読みの対を優先し、原文の文脈や活用形から得た読みを保持するようにしました。
- かな入力の隣接キー、余分に巻き込んだキー、順序違いの探索を改善しました。濁点・半濁点も物理キーとして扱い、語の端で失われていた隣接関係を確認します。打っていないキーを補う候補は低い優先度にしています。
- 前2文字から次の1文字、後ろ2文字から手前の1文字という、ひらがなの並びを判定材料に加えました。確率だけで決めず、品詞や活用との接続を合わせて確認します。
- 候補の順位に、目的語と動作の一般的な関係を反映しました。異様だと判断した範囲では成立する候補を比較し、最上位を採用します。
- 同じ異様な箇所を複数の経路が重ねて修正する問題を修正しました。複数箇所を直す場合も、合成した文で接続と範囲を確認します。
- 数値・単位・記号、読みの注記、入力した文字列の引用、自然な口語表現を誤って補正する問題を改善しました。
- 「学習」メニューから、保存したIMEの表記と読みの対を検索・削除できるようにしました。
- 絵文字などを含む行で、選択・カーソル・補正範囲の位置がずれる問題を修正しました。
- 入力直後に古い解析結果が反映される問題を修正し、変更した行の周辺だけを再確認する処理を改善しました。
- タスクバーやAlt+Tabのアプリアイコンが表示されない場合がある問題を修正しました。
- 解析が途中で止まった結果を完了済みとして再利用しないようにし、エンジンや判定資料の更新時に古い解析キャッシュを使わないようにしました。
- 開発用の診断を整理し、異様判定・読みの由来・候補の順位・棄却理由・後段の変更を追えるようにしました。実際のIME変換を通す隣接誤打と、正常文の検査も拡充しました。

補正は引き続き端末内で行います。実行時にAIへ文章を送信する機能は追加していません。一般語の意味的な分類はAIを使って設計・反証した版付きの資料で、商品名や個人の文章を正解へ置き換える表ではありません。使用回数や使用時刻を記録しない方針、同じキーの重複削除を既定で無効にする設定は維持しています。

補正できない誤変換や、別の語へ補正してしまう例は残っています。特に、辞書にある語同士の意味の違い、語形を説明する引用、IMEで大きく区切りが変わった入力は今後の改善対象です。

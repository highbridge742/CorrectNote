CorrectNote は、誤字を気にせず思考の速度でメモが取れることを目指した Windows 用のメモ帳です。完全にオフラインで動き、辞書とローカルの統計だけで補正します。

v1.5.0 からの変更点です。**保存されるデータから「履歴」を消した版**です。

---

## 使用回数と使用時刻を、記録しなくなりました

これまで CorrectNote は、補正の判断のために「その語を何回使ったか」「最後に使ったのはいつか」を語彙ファイルに記録していました。**この版から、その記録をやめます。**

    覚えるのは  その語を **使ったことがあるか（2回以上書いたか）** だけ
    覚えないのは 回数 ／ 使った日時 ／ 選び直したときの前後の文章

選び直した変換だけは、**最後にどれを選んだかを1つだけ**覚えます。「こうえん」を **講演** に選び直せば次からは **講演** に、あとで **公園** に選び直せばそちらに置き換わります（覚えているのは常に最後の1つで、いつ選んだかも、そのときの前後の文章も残しません）。

### 更新したときに、一度だけ起きること

初めて起動したときに、これまでのファイルが自動で作り直されます。**元に戻すための控えは残しません**（残すと「消す」の意味がなくなるためです）。

| ファイル | どうなるか |
|---|---|
| 語彙（`vocabulary.json`） | 回数と最終使用時刻が**消えます**（語そのものは全部残ります） |
| 選び直しの記録（`choices.json`） | **最後の1つだけ**を残した `last_choice.json` に置き換わります |
| 語のつながりの記録（`context_vec.json`） | **同梱の初期分だけ**に戻ります |
| 文字の並びの記録（`charngram.json`） | **消えます**（同梱の表だけを使います） |
| 打鍵の記録（`ime_readings.json`） | **いまメモに書かれている語の分だけ**が残ります |

打鍵の記録は「アプリに書かれている文字と対」になりました。**メモから文字を消すと、その語について打ったキーの記録も一緒に消えます。** アプリが残す記録は、この決まりで揃えてあります——**いま書かれている本文についての情報だけが残り、本文から消えたものは一緒に消えます。**

### 補正の根拠から「何回」が消えました

候補一覧の「補正根拠」に出ていた `学習した語彙が効いた（「○○」・37回）` という表示は無くなりました。代わりに、本当に効いたときだけ

    学習による選び直し
    前回この読みで選んだ表記に合わせた

と出ます。

## 紫の文字列に「これは正しい」と教えられます

紫（不自然な文字列の印）が付いた場所が、実は正しいこともあります。その範囲を右クリック（または `F2`）すると

    この文字列は正しい（今後、紫を付けない）

が選べるようになりました。以後その文字列には紫が付きません。取り消しは学習メニューの `補正の判断…` からできます。

**紫が消えるだけで、補正は止まりません。** 補正まで止めたいときは、同じ一覧にある `「X」は今後直さない` を選んでください（こちらは前からある機能です）。

## 直した不具合

実際に使っていて出た不具合を直しました。

- **折り返したあとに文字を確定すると、それまでに書いた行の文字がまとめて消えることがある**。原因は、**Shift を押したまま変換を確定した**とき、確定した文字がアプリの「行を選ぶ」キー（Shift+スペース）として読まれ、行を選んだ状態で次の文字が上書きしていたことでした。IME が確定した文字を伴うキーは、すべて「確定」として扱うようにしました。
- **左右のボタンを同時に押したままドラッグすると、そのあとホイールで送れなくなる**。右ボタンを離したときに候補一覧が開いて、ホイールがそちらへ行っていました。押した場所から動いていれば、一覧を開かないようにしました。
- **動作が重い。右ダブルクリックの縮小表示で、重いタブだと戻る範囲が出ず、ドラッグしてもスクロールが反映されない**。縮小表示や右ドラッグの最中も裏で解析が動いていたためです。**触っている間は解析を止める**ようにしました。
- **タブを移ってすぐ検索すると、見つからないことがある**のを直しました。あわせて検索ウインドウを、**前回の検索語が最初から入った状態**で開くようにし、**↓キーで前の検索語をたどれる**ようにしました（この履歴は**保存しません**。アプリを閉じると消えます）。検索ウインドウを開いたままタブを移っても、入力欄に文字を打てる状態が続きます。
- **全部のタブを見終わったあと、編集してから別のタブへ移ると解析がやり直しになる**のを直しました。解析結果の控えが、同じタブの古い本文で押し出されていました。

## 補正が良くなりました

回数という証拠を手放した代わりに、**文の形から判断する**部分を厚くしました。

- **web から貼り付けた文章を壊さなくなりました**。`強炭酸` が `今日探索`、`焙煎度` が `浴び選択`、`効き目は？` が `決めは？` のように、**正しく書いてある語の並びが別の語に置き換わる**ことがありました。
- **動詞・形容詞の活用を含むひらがなの並び**を壊さなくなりました（`あまりにもひどくて` → `ひとくち`、`飲むのをやめたら` → `やたら`）。
- 直せないところにも、**気づいたという印（紫）は出す**ようにしました。格助詞が続く並び（`もみとに`）や、`セク` のようなカタカナ2字の断片に印が付きます。
- そのほか、濁点の位置ずれ・打鍵の重複・脱字・順序違いの当て方を見直しました。

### 測った結果

**入れたばかりの状態（初期状態）**で、v1.5.0 と同じ材料に通して比べました。

    読みを壊した誤字 3,481 件   かな     直った 2,003 → **2,052**／化けた 111 → **113**
                                 ローマ字 直った 2,004 → **2,053**／化けた 105 → **106**
    未解決リスト 29 行           かな ◎19 → **◎22**／ローマ字 ◎17 → **◎20**
    正しい日本語 1,500 文        誤検知 **0 件**（変わらず）
    同梱の見本 40 件             直る **39**／壊した **0**（変わらず）

誤字の標本は語彙から作るため版ごとに中身が変わります。上の数字は、**v1.5.0 が作った材料をそのまま両方の版に通した**ものです。

**化けが 1〜2 件増えています。** このアプリは「正しく書いたものを壊さない」を「直る」より優先するので、増えたぶんは次に持ち越します。

## 内部の変更

- 「どちらの表記が一般的か」を、**同梱の費用表**と、費用が同点のときは **2字の漢語の「日常語かどうか」の表**（AI の判断で作ったもの）で決めるようにしました
- カタカナ語の候補が拮抗したときは、**同じ文字の2連打**（`クリッック` の `ッ`）を先に疑うようにしました
- 語彙の並び順から時刻の要素が消えたので、**同じ入力には必ず同じ答え**が返るようになりました
- 同じ表記に複数の読みがあるとき（`機能` の `きのう` と `きのー`）、**正しい読みのほうを名乗る**ように直しました。ここがずれると「読みを変えずに表記だけ差し替える」誤りを止める見張りが効かなくなります
- 外来語・英語の直し先の表が、**選び直しを見ていなかった**のを直しました（`ばいおりん` を `ヴァイオリン` に選び直しても表だけ古いままでした）
- ひらがなの並びが「日本語として読める形か」を、**語の表と活用の文法**で判定するようにしました。読める並びは、明らかに自然にならない限り触りません
- 数字の直後の助数詞（`5つ`）・`ている` の縮約（`されてる`）・敬語の接頭辞（`ご相談`）を、文の部品として数えるようにしました
